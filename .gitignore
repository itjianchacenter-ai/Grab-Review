# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
build/
.next/
out/
next-env.d.ts

# Env files
.env
.env.local
.env.*.local

# Database
*.db
*.db-journal
prisma/dev.db*

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Claude Code local session settings
.claude/

# Encrypted credentials vault (do NOT commit)
vault.enc

# User auth + sessions (do NOT commit)
users.json
.sessions.json
scripts/init-users.py

# Watchdog config (Slack webhook URL — do NOT commit)
scripts/watchdog.env

# Runner artifacts (cookies, logs, secrets)
runner/.env
runner/profiles/
runner/chrome-profiles/
runner/auth-profiles/
runner/logs/
runner/node_modules/
runner/branches.csv
runner/branches.json
cookies/

# Server data (contains scraped menu + reviews — customer PII)
server-data.json
server-reviews.json

# Local backups
*.bak
*.bak.*
vault.enc.before-restore.*
scripts/watchdog.env.save

# User's local experimental scripts (may contain hardcoded secrets)
runner/cap*.js
runner/capture-*.js

# Backups: ignore everything except encrypted backups
backups/*
!backups/*.tar.gz.enc
